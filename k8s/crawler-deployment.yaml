apiVersion: apps/v1
kind: Deployment
metadata:
  name: crawler-service
  namespace: production
  labels:
    app: crawler-service
spec:
  replicas: 2
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      app: crawler-service
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: crawler-service
    spec:
      containers:
        - name: crawler-service
          image: rahijamil/viralis-crawler:latest
          ports:
            - containerPort: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 3
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
            failureThreshold: 6
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "250m"
          env:
            - name: ENV
              value: production
---
apiVersion: v1
kind: Service
metadata:
  name: crawler-service
  namespace: production
spec:
  selector:
    app: crawler-service
  ports:
    - port: 80
      targetPort: 8080
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: crawler-service-pdb
  namespace: production
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: crawler-service
